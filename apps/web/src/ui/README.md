# UI primitives

Conventions:
- Use class-variance-authority (CVA) for variants and sizes.
- Compose class names with `cn` from `src/lib/cn`.
- No hard-coded copy: all visible strings and aria labels come from `next-intl`.
- Prefer Radix primitives for accessibility and keyboard support.
